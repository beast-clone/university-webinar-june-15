# No proxy rules currently — both /amc-pathway/ and /nz-pathway/ are physically
# served from folders inside this repo (see README.md). Add rules here only when
# a country launches as a separate Netlify site.
